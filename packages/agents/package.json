{
  "name": "@partyco/agents",
  "version": "0.0.0",
  "private": true,
  "description": "Provider layer: API-key and delegated-CLI transports. Runs on the member's machine, never holds a vendor credential.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mrdy01/PartyCo.git"
  },
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "test": "node --test test.mjs"
  }
}
